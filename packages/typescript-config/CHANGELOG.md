# @qpmatrix/typescript-config

## 0.2.0

### Minor Changes

- Remade in qpm-ui (QPMSEC-433). Ported verbatim from
  `qpmatrix-packages@c498f95` (`packages/typescript-config`, 0.1.0)
  into this package's own public repo — same `base.json`,
  `react-library.json`, `api.json`, `nextjs.json` compiler options.
  No config values changed; only the repo and version numbering moved.

## 0.1.0 (qpmatrix-packages)

### Minor Changes

- Initial release of shared QPMatrix packages for development (v0.1.0).
