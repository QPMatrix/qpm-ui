/**
 * QPSegmentedControl — public surface of this folder.
 *
 * Consumers import from the folder, never from a file inside it, so the
 * markup/types/constants/utils split stays an implementation detail.
 */
export * from "./segmented-control";
export * from "./segmented-control.constants";
export type * from "./segmented-control.types";
export * from "./segmented-control.utils";
