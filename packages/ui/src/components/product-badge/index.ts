/**
 * QPProductBadge — public surface of this folder.
 *
 * Consumers import from the folder, never from a file inside it, so the
 * markup/types/constants/utils split stays an implementation detail.
 */
export * from "./product-badge";
export * from "./product-badge.constants";
export type * from "./product-badge.types";
export * from "./product-badge.utils";
