/**
 * QPTypingIndicator — public surface of this folder.
 *
 * Consumers import from the folder, never from a file inside it, so the
 * markup/types/constants/utils split stays an implementation detail.
 */
export * from "./typing-indicator";
export * from "./typing-indicator.constants";
export type * from "./typing-indicator.types";
export * from "./typing-indicator.utils";
